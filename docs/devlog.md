- ## 2026-09-18

  ### 오늘 목표
  - 1인칭 시점 구조 이해
  - 1인칭 시점 구현

  ### 새로 배운 것
  - 1인칭 시점에서 카메라를 좌우로 움직이는 것은 몸 전체가 y축 기준으로 움직이는 것이고 상하로 움직이는 것은 고개만 위 아래로 움직이는 것.
  - Mouse Delta값은 마우스의 절대 위치가 아니라 프레임마다 움직인 양이다.
  - `Rotate()`는 현재 회전에 값을 추가하는 상대 회전이고, `rotation/localRotation = Quaternion.Euler()`는 계산한 특정 회전값으로 직접 설정하는 방식이다.
  - Math.Clamp(value, min, max) 는 value값을 min~max로 통제하는 함수이다

  ### 다음 목표
  - wasd 이동 구현

    